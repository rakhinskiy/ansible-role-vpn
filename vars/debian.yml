# code: language=ansible
---

# 00 # Always

vpn_always_packages:
  - "apt-transport-https"
  - "ca-certificates"
  - "gnupg2"
  - "python3-apt"

# 02 # Configure

vpn_ipsec_config_file: "/etc/ipsec.conf"
vpn_ipsec_secrets_file: "/etc/ipsec.secrets"

vpn_ipsec_service: "ipsec"
